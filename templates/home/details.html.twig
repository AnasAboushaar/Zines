{% extends 'base.html.twig' %}

{% block title %}Magazines !{% endblock %}

{% block body %}

    <style>
        .colorCa
    </style>

    <h1>{{ magazine.name }}</h1>
    <p>Prix : {{ magazine.price }}€</p>
    <p>Date de création : {{ magazine.createdAt|date('d.m.Y') }}</p>
    <p class="colorCategory">Catégorie : {{ magazine.category.name }}</p>

{% endblock %}
